# Allow legacy peer dependencies to resolve React version conflicts
# This is needed for React Native projects where some dependencies
# may require slightly different React versions
legacy-peer-deps=true
