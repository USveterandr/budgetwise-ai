{
  "name": "budgetwise-app",
  "version": "1.0.0",
  "description": "BudgetWise - AI-powered financial management SaaS platform",
  "private": true,
  "scripts": {
    "preinstall": "node preinstall.js && cd frontend && npm install --legacy-peer-deps",
    "build": "./build.sh",
    "build-fallback": "rm -rf yarn.lock frontend/yarn.lock && npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps && npm run build && cp ../_headers build/ && cp ../_redirects build/",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm start",
    "install:frontend": "cd frontend && npm install --legacy-peer-deps",
    "install:all": "npm run install:frontend",
    "postbuild": "echo 'Build completed successfully! Deploy the frontend/build directory to Cloudflare Pages.'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/USveterandr/budgetwise-ai"
  },
  "keywords": [
    "budgeting",
    "finance",
    "saas",
    "react",
    "fastapi"
  ],
  "author": "BudgetWise Team",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0",
    "yarn": ">=1.0.0"
  },
  "preferGlobal": false,
  "dependencies": {
    "mongodb": "^6.19.0"
  }
}
