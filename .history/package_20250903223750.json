{
  "name": "budgetwise-client",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd budgetwise-app/client && npm ci && npm run build && rm -rf ../../frontend/build && cp -r build/ ../../frontend/",
    "start": "cd budgetwise-app/client && npm start --https --port 3001",
    "test": "cd budgetwise-app/client && npm test",
  "dev": "npm run start"
},
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
"react-scripts": "5.0.1",
"typescript": "^4.9.5"
  }
}
